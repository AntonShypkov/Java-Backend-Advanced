spring:
  datasource:
    url: jdbc:h2:mem:env:test
    driver-class-name: org.h2.Driver
    username: SA_test
    password:

custom:
  datasource:
    enabled: true
    url: jdbc:h2:mem:env:test:custom

debug: false